# Book Generation Flow Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           USER JOURNEY                                   │
└─────────────────────────────────────────────────────────────────────────┘

1. User creates order → Places order with personalization
                      ↓
2. Order created     → order_items.generation_status = 'pending'
                      ↓
3. Backend picks up  → Status changes to 'processing'
                      ↓
4. Book generated    → PDF created and uploaded
                      ↓
5. Status updated    → generation_status = 'completed'
                     → pdf_url = 'https://...'
                      ↓
6. Notifications sent → Email + Push + In-app
                      ↓
7. User sees update  → "Book Ready!" in My Orders
                      ↓
8. User downloads    → Click download button
                      ↓
9. PDF opens         → Opens in default viewer


┌─────────────────────────────────────────────────────────────────────────┐
│                         DATABASE SCHEMA                                  │
└─────────────────────────────────────────────────────────────────────────┘

order_items table:
┌──────────────────────────────────────────────────┐
│ id                     UUID (PK)                 │
│ order_id               UUID (FK → orders)        │
│ book_id                INT                       │
│ quantity               INT                       │
│ unit_price             DECIMAL                   │
│ personalization_data   JSONB                     │
│ ────────────────────── NEW FIELDS ──────────────│
│ generation_status      TEXT (pending/processing/ │
│                             completed/failed)    │
│ pdf_url                TEXT (nullable)           │
│ generated_at           TIMESTAMP (nullable)      │
│ generation_error       TEXT (nullable)           │
│ updated_at             TIMESTAMP                 │
└──────────────────────────────────────────────────┘

notifications table:
┌──────────────────────────────────────────────────┐
│ id                     UUID (PK)                 │
│ user_id                UUID (FK → users)         │
│ title                  TEXT                      │
│ message                TEXT                      │
│ type                   TEXT (book_completed/etc) │
│ metadata               JSONB                     │
│ is_read                BOOLEAN                   │
│ read_at                TIMESTAMP                 │
│ created_at             TIMESTAMP                 │
└──────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                     COMPONENT INTERACTION                                │
└─────────────────────────────────────────────────────────────────────────┘

┌────────────────┐
│   User Places  │
│     Order      │
└────────┬───────┘
         │
         ▼
┌────────────────────────────────────────┐
│    OrderService.createOrder()          │
│  ─ Creates order with items            │
│  ─ Sets generation_status = 'pending'  │
└────────┬───────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│              BACKEND PROCESSOR                          │
│  (Your backend service - to be implemented)             │
│                                                          │
│  1. Monitor pending items                               │
│  2. Update status to 'processing'                       │
│  3. Call AI service to generate book                    │
│  4. Upload PDF to Supabase Storage                      │
│  5. Update status to 'completed' with pdf_url           │
│  6. Trigger NotificationService                         │
└────────┬────────────────────────────────────────────────┘
         │
         ├─────────────────────────────────┐
         │                                 │
         ▼                                 ▼
┌──────────────────────┐        ┌──────────────────────┐
│ NotificationService  │        │  User Views Order    │
│  - sendEmail()       │        │   in My Orders       │
│  - sendPush()        │        └─────────┬────────────┘
│  - createInApp()     │                  │
└──────────────────────┘                  ▼
                              ┌─────────────────────────┐
                              │  Status Displayed:      │
                              │  ┌───────────────────┐  │
                              │  │ ✓ Book Ready!    │  │
                              │  │   [Download]     │  │
                              │  └───────────────────┘  │
                              └─────────┬───────────────┘
                                        │
                                        ▼
                              ┌──────────────────────────┐
                              │ BookGenerationService    │
                              │  .downloadBook()         │
                              │  - Opens PDF in viewer   │
                              └──────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                        UI STATUS FLOW                                    │
└─────────────────────────────────────────────────────────────────────────┘

Order Item Card
┌──────────────────────────────────────────────────────┐
│  [📕 Cover]   My Adventure Book                      │
│               Personalized for: Alex                 │
│               Qty: 1          $29.99                 │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │ [⏱] In Queue                                    │ │
│  │     Your book generation will start soon        │ │
│  └─────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
                     ↓ (Processing)
┌──────────────────────────────────────────────────────┐
│  [📕 Cover]   My Adventure Book                      │
│               Personalized for: Alex                 │
│               Qty: 1          $29.99                 │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │ [⟳] Generating Book...                          │ │
│  │     We're creating your personalized book       │ │
│  └─────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
                     ↓ (Completed)
┌──────────────────────────────────────────────────────┐
│  [📕 Cover]   My Adventure Book                      │
│               Personalized for: Alex                 │
│               Qty: 1          $29.99                 │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │ [✓] Book Ready!               [Download ↓]      │ │
│  │     Your personalized book is ready to download │ │
│  └─────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                    FILE DEPENDENCIES                                     │
└─────────────────────────────────────────────────────────────────────────┘

my_orders_page.dart
    ├── uses → order_service.dart
    ├── uses → book_generation_service.dart
    ├── uses → localization_service.dart
    └── renders → OrderItem (from models/order.dart)

order_service.dart
    ├── queries → Supabase (orders, order_items tables)
    └── returns → Order & OrderItem models

book_generation_service.dart
    ├── updates → Supabase (order_items table)
    ├── uses → url_launcher (for downloads)
    └── streams → Real-time status updates

notification_service.dart
    ├── calls → Edge Functions (email, push)
    ├── creates → In-app notifications
    └── uses → Supabase (notifications table)

models/order.dart
    ├── defines → Order class
    ├── defines → OrderItem class
    └── includes → Generation status fields


┌─────────────────────────────────────────────────────────────────────────┐
│                   NOTIFICATION WORKFLOW                                  │
└─────────────────────────────────────────────────────────────────────────┘

When book generation completes:

1. Backend → Calls NotificationService.notifyBookCompletion()
              │
              ├─→ Email Service (via Edge Function)
              │   └─→ Sends formatted HTML email
              │       ├─ Subject: "Your Book is Ready!"
              │       ├─ Body: Book details + Download link
              │       └─ CTA Button: "Download Your Book"
              │
              ├─→ Push Notification Service (via Edge Function)
              │   └─→ Sends mobile/web push notification
              │       ├─ Title: "Your Book is Ready! 📚"
              │       ├─ Body: Book title + child name
              │       └─ Action: Opens My Orders
              │
              └─→ In-App Notification (direct DB insert)
                  └─→ Creates notification record
                      ├─ Stored in notifications table
                      ├─ Shows in app notification center
                      └─ Badge count updates


┌─────────────────────────────────────────────────────────────────────────┐
│                 RESPONSIVE DESIGN LAYOUT                                 │
└─────────────────────────────────────────────────────────────────────────┘

Mobile (<650px):
┌──────────────────┐
│  [My Orders]     │
│                  │
│ ┌──────────────┐ │
│ │ Order #1234  │ │
│ │ ─────────── │ │
│ │ [Book Item] │ │
│ │ [Status]    │ │
│ │ [Download]  │ │
│ └──────────────┘ │
│                  │
│ ┌──────────────┐ │
│ │ Order #1235  │ │
│ └──────────────┘ │
└──────────────────┘

Tablet/Web (≥650px):
┌─────────────────────────────────┐
│        [My Orders]              │
│                                 │
│    ┌─────────────────────┐     │
│    │ Order #1234         │     │
│    │ ─────────────────── │     │
│    │ [Book] [Status]     │     │
│    │        [Download]   │     │
│    └─────────────────────┘     │
│                                 │
│    ┌─────────────────────┐     │
│    │ Order #1235         │     │
│    └─────────────────────┘     │
└─────────────────────────────────┘
    Max Width: 1000px (centered)


┌─────────────────────────────────────────────────────────────────────────┐
│                     ERROR HANDLING FLOW                                  │
└─────────────────────────────────────────────────────────────────────────┘

Download Attempt
      │
      ├─ PDF URL invalid?
      │   └─→ Show error: "Invalid download link"
      │
      ├─ Network error?
      │   └─→ Show error: "Network error, try again"
      │
      ├─ Storage access denied?
      │   └─→ Show error: "Access denied"
      │
      └─ Success?
          └─→ Show success: "Opening book..."
          └─→ Open PDF in viewer


┌─────────────────────────────────────────────────────────────────────────┐
│                 LOCALIZATION SUPPORT                                     │
└─────────────────────────────────────────────────────────────────────────┘

Supported Languages:
├─ English (en.json)
│   ├─ my_orders_page_generation_completed
│   ├─ my_orders_page_generation_processing
│   ├─ my_orders_page_generation_pending
│   ├─ my_orders_page_generation_failed
│   └─ my_orders_page_download
│
└─ Arabic (ar.json)
    ├─ RTL text direction support
    └─ Same keys with Arabic translations


┌─────────────────────────────────────────────────────────────────────────┐
│                    SECURITY CONSIDERATIONS                               │
└─────────────────────────────────────────────────────────────────────────┘

1. Row Level Security (RLS):
   ├─ Users can only see their own orders
   ├─ Users can only download their own PDFs
   └─ Notifications are user-specific

2. Storage Security:
   ├─ PDFs stored in user-specific folders
   ├─ Public access only via signed URLs (optional)
   └─ Automatic expiration for sensitive content

3. Data Privacy:
   ├─ Child names and images encrypted in transit
   ├─ GDPR-compliant data handling
   └─ User consent for notifications
